{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "java (buildArtifact)",
			"targetPath": "${workspaceFolder}/${workspaceFolderBasename}.jar",
			"elements": [
				"${compileOutput}",
				"${dependencies}"
			],
			"problemMatcher": [],
			"label": "java (buildArtifact): fwTest",
			"options": {
                "compilerArgs": [
                    "-parameters"
                ]
            },
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}
